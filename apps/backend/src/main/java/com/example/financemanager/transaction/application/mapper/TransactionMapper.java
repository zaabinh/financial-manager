package com.example.financemanager.transaction.application.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface TransactionMapper {
    // TODO: Add transaction entity/DTO mappings.
}
